html
  head
    link(rel="stylesheet", href="/css/styles.css")
    link(rel="stylesheet", href="/css/update.css")
    block title
      title Poetika

  body

    #logo
      img#img(src="/img/poetikablack.png")
    #bienvenida
      p| ¡Bienvenido a POETIKA! Sumérgete en el rico mundo de la poesía vasca mientras exploras nuestra base de datos. Registrate  y conecta con la profundidad de la tradición poética vasca!
    block content
     div#register-container
      h1 Register
      if error
        p(class="error")= error
      form(action="/register", method="post")
        label(for="email") email
        input#email(type="email", name="email")
        label(for="password") contraseña
        input#password(type="password", name="password")
        label(for="passwordRepeat") repetir contraseña
        input#passwordRepeat(type="password", name="passwordRepeat")
        button(type="submit") Register
