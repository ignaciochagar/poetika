extends ../layout

block content 
    if error 
        p(class="error")= error
    if artist
        form(action="/poem/"+poem.poem_id, method="post") 
            label(for="title") Title 
            input(id="title" type="text" name="title" value=poem.title) 
            label(for="author") Author
            input#author(type="text", name="author" checked=poem.author)
            label(for="year_release") Year release
            input#year_release(type="number", name="year_release" value=poem.year_release)  
            button(type="submit") Guardar

